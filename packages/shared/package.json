{
  "name": "@tierstack/shared",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "//": "The barrel pulls in ids.ts, which needs node:crypto — importing it from a browser bundle fails to build. ./money is the subpath for the parts that are safe anywhere: currency metadata and minor-unit arithmetic, no Node built-ins. Add a subpath rather than dropping the barrel, so server code keeps its one import.",
  "exports": {
    ".": "./src/index.ts",
    "./money": "./src/money.ts",
    "./interval": "./src/interval.ts",
    "./package.json": "./package.json"
  },
  "scripts": { "typecheck": "tsc --noEmit" },
  "dependencies": { "zod": "^3.24.1" }
}
